//import React from 'react';
//import { shallow } from "enzyme";
//import Contacts from '../components/Contacts';
//import ContactsClass from '../components/Contacts-Class';

describe('Contact Component test cases', () => {
  //let component;
  //beforeEach(() => {
  //  component = shallow(<Contacts />);
  //})
  //it('Snapshot create/update test case for Contacts', () => {
  //  expect(component).toMatchSnapshot();
  //})

  // 4.Elements rendering - Render paragraph 1 time in Contacts Component (toBe(1))

  // 4.1Render heading of Contacts Component - toBe(true)

  // 5.render props - Render input props of Contacts Component (.props() - toEqual(''))


  // 6.render state in functional components - Render version state in Contacts Component (.text() - toEqual('1'))


  // 7.render state in class components - Render version state in Contacts Component (.state, toEqual)

  // 7.1. Render version state in Contacts Component

  // 8.functional testing - Function in Contacts Component (button click simulate toEqual)
})
